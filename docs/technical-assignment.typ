#import "../template/fqw.typ": *

#show: fqw-document

#fqw-header-abstract()

Техническое задание устанавливает требования к разработке программного
модуля для сетевого симулятора Ns-3, предназначенного для моделирования
программно-аппаратного гидроакустического приёмопередатчика и
гидроакустического канала связи с целью проведения виртуальных и
полунатурных испытаний и исследований сценариев эксплуатации.
Техническое задание включает в себя страниц~--~33, приложений~--~5. // ToDo написать автоматические функции в будущем.

Список ключевых слов:
моделирование,
виртуальные испытания,
полунатурные
испытания,
сетевой симулятор,
Ns-3,
гидроакустический приёмопередатчик,
гидроакустический канал связи.

#pagebreak()

#fqw-outline()

#fqw-introduction(label: <sec:introduction>, heading-counter: 1)

== Наименование программы
#fqw-indent-before-text
<sec:program_name>

Наименование программы: «Программный модуль модели гидроакустического
приёмопередатчика для Ns-3».

== Область применения
#fqw-indent-before-text
<sec:application_area>

Модель применяется для:
- Виртуальных испытаний и исследовательского моделирования
  гидроакустических приёмопередатчиков и каналов связи;
- Воспроизведения сценариев передачи сообщений между подводными объектами/узлами в условиях,
  характерных для гидроакустики;
- Отработки алгоритмов верхнего уровня на базе результатов моделирования
  канала и приёмопередатчика.

При этом модуль предназначен для предоставления
сетевой и физической модели гидроакустического канала связи и
обмена сообщениями между узлами
в среде ns-3.
Алгоритмы верхнего уровня в состав модуля не входят и
могут разрабатываться и отрабатываться на его базе с использованием
предоставляемых средств моделирования.

Модуль ориентирован на применение инженерами и исследователями организации,
занимающейся гидроакустикой, на рабочих станциях специалистов.

= Основание для разработки
#fqw-indent-before-text
<sec:development_basis>

Разработка программы ведётся на основании задания на выпускную работу
бакалавра, полученного в соответствии с приказом №1203-ст от 05 сентября
2025 года «Об утверждении тем и руководителей выпускных работ
бакалавров» по кафедре ПОАС на тему «Разработка компьютерной модели
программно-аппаратного гидроакустического приёмопередатчика для его
виртуальных испытаний» и согласовано с ФГУП ВНИИА им. Духова НПЦ
«Гидросвязь».

= Назначение разработки
#fqw-indent-before-text
<sec:development_purpose>

Цель разработки: сократить расходы и время на разработку систем
гидроакустической связи за счёт разработки компьютерной модели
программно-аппаратного гидроакустического приёмопередатчика для
виртуальных испытаний систем гидроакустической связи и отработки
алгоритмов функционирования мультиагентных систем.

Программный модуль предназначен для эксплуатации инженерами и
программистами в ФГУП ВНИИА им. Духова НПЦ «Гидросвязь».

= Требования к программе
<sec:program_requirements>

== Требования к функциональным характеристикам
<sec:functional_requirements>

=== Состав программного модуля
#fqw-indent-before-text
<sec:module_composition>

Программный модуль должен включать:
- Модель гидроакустического приёмопередатчика;
- Модель гидроакустического канала связи;
- Набор базовых компонентов, необходимых для реализации других моделей
  гидроакустических приёмопередатчиков;
- Набор базовых компонентов, необходимых для реализации других моделей
  гидроакустических каналов связи.

Программный модуль должен содержать
не только конкретные реализации моделей,
но и предлагать компоненты и архитектурные подходы,
необходимыедля реализации других моделей.

=== Требования к архитектуре модели программно-аппаратного приёмопередатчика
<sec:transceiver_architecture_requirements>

==== Общие требования
#fqw-indent-before-text
<sec:general_requirements>

Архитектура модели приёмопередатчика должна обеспечивать реализацию
физического и канального уровней устройства связи по модели OSI.

Архитектура модели приёмопередатчика должна обеспечивать совместимость с
моделями каналов гидроакустической связи.

Архитектура модели приёмопередатчика должна обеспечивать возможность
реализации конкретных моделей приёмопередатчиков без необходимости
изменения интерфейсов базовых классов.

==== Независимость уровней
#fqw-indent-before-text
<sec:level_independence>

Физический и канальный уровень приёмопередатчика должны быть разделены.

Взаимодействие между уровнями должно быть организовано через чётко
определённые интерфейсы и регламентированные механизмы обратного вызова.

Архитектура физического уровня должна быть независима от архитектуры
канального уровня.

==== Функции физического уровня
#fqw-indent-before-text
<sec:physical_layer_functions>

Физический уровень приёмопередатчика должен обеспечивать:

- Формирование передаваемого сигнала на основе
  входящего информационного пакета;

- Сигнал на физическом уровне описывается
  спектральной плотностью мощности и его продолжительностью;

- Передачу сигнала в модель гидроакустического канала связи;

- Приём сигнала из модели гидроакустического канала связи;

- Обработку принятого сигнала и извлечение полезной нагрузки
  (полезной информации) из принятого сигнала.

==== Модели интерференции
#fqw-indent-before-text
<sec:interference_models>

Архитектура приёмопередатчика должна поддерживать работу с
различными моделями интерференции.

Модель интерференции должна рассчитывать значение отношения сигнал/шум (ОСШ)
для каждого входящего сигнала,
который потенциально может быть принят и декодирован.

Решение о том, будет ли корректно принят и декодирован входящий сигнал,
принимается в конкретных реализациях моделей приёмопередатчиков,
в том числе на основании рассчитанных моделями интерференции
характеристик качества приёма сигнала.

=== Требования к архитектуре модели гидроакустического канала связи
#fqw-indent-before-text
<sec:channel_architecture_requirements>

Архитектура модели гидроакустического канала связи должна обеспечивать
взаимодействие с физическим уровнем модели приёмопередатчика, включая
передачу сигналов от физического уровня в модель среды распространения и
обратно.

Архитектура канала должна предусматривать возможность создания
конкретных реализаций моделей каналов связи.

// Warn: в работе осуществляется только расчёт потерь распространения и затухания
Расчёт таких эффектов, как задержка, затухание, многолучёвость, эффект
Доплера, должен осуществляться через регламентированные интерфейсы.
Модели каналов связи для расчёта свойств распространения сигналов должны
использовать эти интерфейсы.

=== Требования к реализации модели приёмопередатчика
<sec:transceiver_implementation_requirements>

==== Общие положения
#fqw-indent-before-text
<sec:general_provisions>

Модель приёмопередатчика должна быть основана на архитектуре
программно-аппаратного гидроакустического приёмопередатчика,
требования к которой были описаны ранее.

Модель приёмопередатчика должна реализовывать
физический и канальный уровень модели OSI.

Модель приёмопередатчика поддерживает только один формат
передаваемого и принимаемого сигнала.
Сигналы другого типа приёмопередатчик принимать не должен.

==== Состояния физического уровня
#fqw-indent-before-text
<sec:physical_layer_states>

Физический уровень модели приёмопередатчика должен реализовывать
конечный автомат со следующими состояниями:

- OFF -- устройство отключено (передача и приём не выполняются);

- IDLE -- устройство находится в режиме ожидания и осуществляет
  прослушивание канала, готово к отправке и приёму сигналов;

- RX -- устройство находится в состоянии
  приёма и обработки входящего сигнала;

- TX -- устройство находится в состоянии передачи сигнала.

Физический уровень модели должен находиться только в одном состоянии в
каждый момент модельного времени.

Базовая реализация приёмопередатчика должна обеспечивать режим
half-duplex: одновременные передача и приём не допускаются.

Подробное описание поведения конечного автомата физического уровня
приведено на диаграмме состояний, представленной на рисунке~#fqw-figure-ref(<fig:phy-state-machine>).

==== Функции канального уровня
#fqw-indent-before-text
<sec:data_link_layer_functions>

Канальный уровень модели приёмопередатчика должен
обеспечивать буферизацию данных на передачу в виде очереди пакетов.
Пользователь должен иметь возможность задать максимальный размер очереди
(определяется количеством пакетов).
При достижении максимального размера очереди,
новые пакеты должны быть отброшены.

Приёмопередатчик должен иметь MAC-адрес
(уникальный идентификатор канального уровня).

MAC-адрес состоит из 8 бит.

Канальный уровень модели приёмопередатчика должен добавлять к
передаваемым данным заголовок, содержащий:

- MAC-адрес отправителя;

- MAC-адрес получателя.

==== Используемая модель интерференции
#fqw-indent-before-text
<sec:interference_model_implementation>

Модель интерференции должна вычислять отношение сигнал к шуму по
следующим формулам
(см.~формулы~#fqw-eq-ref(<eq:signal_power>)--#fqw-eq-ref(<eq:sinr>)):

$ P_s = integral_(f_1)^(f_2) S_"rx"(f) d f $ <eq:signal_power>
#fqw-where((
  ($P_s$, [мощность полезного сигнала в рабочей полосе, Па²]),
  ($f_1$, [нижняя граница рабочей полосы, Гц]),
  ($f_2$, [верхняя граница рабочей полосы, Гц]),
  ($S_"rx"$, [спектральная плотность мощности полезного сигнала на входе приёмника, Па²/Гц]),
))

#fqw-equation-list((
  ([$ P_n = integral_(f_1)^(f_2) S_n(f) d f $], <eq:noise_power>),
  ([$ P_i = integral_(f_1)^(f_2) S_i(f) d f $], <eq:interference_power>),
))
#fqw-where((
  ($P_n$, [мощность шума в рабочей полосе, Па²]),
  ($S_n$, [спектральная плотность мощности шума, Па²/Гц]),
  ($P_i$, [мощность помехи \(другого сигнала) в рабочей полосе, Па²]),
  ($S_i$, [спектральная плотность мощности помехи на входе приёмника, Па²/Гц]),
))

$ S I N R = P_s / (sum_(k = 1)^(N_i) P_(i,k) + P_n) $ <eq:sinr>
#fqw-where((
  ($S I N R$, [отношение мощности полезного сигнала к суммарной мощности шума и помех]),
  ($P_s$, [мощность полезного сигнала в рабочей полосе, Па²]),
  ($P_n$, [мощность шума в рабочей полосе, Па²]),
  ($N_i$, [число мешающих передач \(других сигналов)]),
  ($P_(i,k)$, [мощность k-й помехи \(другого сигнала) в рабочей полосе, Па²]),
))

==== Решение об успешном приёме и декодировании сигнала
#fqw-indent-before-text
<sec:successful_reception_decision>

Модель приёмопередатчика считает сигнал успешно принятым и декодированным,
если тип сигнала соответствует типу,
который может декодировать этот приёмопередатчик,
и значение ОСШ для этого сигнала выше порогового значения,
заданного пользователем
(см.~формулу~#fqw-eq-ref(<eq:successful_reception_threshold>)):

$ S I N R >= gamma_"th" $ <eq:successful_reception_threshold>
#fqw-where((
  ($S I N R$, [отношение мощности полезного сигнала к суммарной мощности шума и помех]),
  ($gamma_"th"$, [порог успешного приёма]),
))

==== Трассировка
#fqw-indent-before-text
<sec:tracing>

Модель приёмопередатчика должна поддерживать вывод результатов
трассировки в формате pcap.
Файл должен содержать информацию о принятых и отправленных пакетах.

=== Требования к модели гидроакустического канала связи
<sec:channel_model_requirements>
#fqw-indent-before-text

==== Расчёт характеристик распространения
#fqw-indent-before-text
<sec:propagation_characteristics_calculation>

Базовая модель канала связи должна обеспечивать:

- расчёт времени распространения сигнала
  от точки излучения до точки приёма;

- расчёт затухания (потерь) сигнала при прохождении
  из точки излучения в точку приёма.

==== Расчёт времени распространения
#fqw-indent-before-text
<sec:propagation_delay_calculation>

Базовая модель канала должна осуществлять расчёт
времени распространения сигнала на основании 2 заданных параметров:

- Координата излучателя в трёхмерном пространстве;

- Координата приёмника в трёхмерном пространстве.

Для расчёта времени распространения считать:

- Скорость распространения звука в воде постоянная;

- Сигнал однолучевой;

- Расстояние между узлами определяется как евклидово расстояние между их
  координатами (см. формулу #fqw-eq-ref(<eq:euclidean_distance>)).

$ d = norm(r_"rx" - r_"tx") $ <eq:euclidean_distance>
#fqw-where((
  ($d$, [расстояние между передатчиком и приёмником, м]),
  ($r_"tx"$, [радиус-вектор передатчика]),
  ($r_"rx"$, [радиус-вектор приёмника]),
))

Время распространения определяется по формуле
#fqw-eq-ref(<eq:propagation_delay>):

$ tau = d / c $ <eq:propagation_delay>
#fqw-where((
  ($tau$, [задержка распространения сигнала, с]),
  ($d$, [расстояние между передатчиком и приёмником, м]),
  ($c$, [скорость звука в воде, м/с]),
))

==== Расчёт затухания \(потерь) сигнала
#fqw-indent-before-text
<sec:signal_attenuation_calculation>

Базовая модель канала должна осуществлять расчёт затухания сигнала
на основании 3 заданных параметров:

- координата излучателя в трёхмерном пространстве;

- координата приёмника в трёхмерном пространстве;

- мощность сигнала на выходе излучателя.

Модель потерь состоит из нескольких составляющих
(см. формулу #fqw-eq-ref(<eq:propagation_loss>)):

$ T L(f, d) = T L_"geom"(d) + T L_"abs"(f, d) $ <eq:propagation_loss>
#fqw-where((
  ([$T L$ (Transmission loss)], [суммарные потери сигнала на частоте $f$ при расстоянии $d$, дБ]),
  ($T L_"geom"$, [потери на геометрическое расхождение, дБ]),
  ($T L_"abs"$, [потери на абсорбцию, дБ]),
  ($f$, [частота сигнала, Гц]),
  ($d$, [расстояние, пройденное сигналом, м]),
))

Потери на геометрическое расхождение вычисляются по формуле
#fqw-eq-ref(<eq:geometric_loss>):

$ T L_"geom"(d) = 10 n log_10 d $ <eq:geometric_loss>
#fqw-where((
  ($T L_"geom"$, [потери на геометрическое расхождение, дБ]),
  ($n$, [коэффициент расхождения]),
  ($d$, [расстояние, пройденное сигналом, м]),
))

Коэффициент n является настраиваемым и находится в диапазоне
n~∈~\[1;2\].

Потери на абсорбцию вычисляются по формуле
#fqw-eq-ref(<eq:absorption_loss>):

$ T L_"abs"(f, d) = alpha(f) dot d dot 10^3 $ <eq:absorption_loss>
#fqw-where((
  ($T L_"abs"$, [потери на абсорбцию, дБ]),
  ($alpha$, [коэффициент поглощения, дБ/км]),
  ($d$, [расстояние, пройденное сигналом, м]),
))

Для вычисления коэффициента поглощения используются формулы
Торпа (см.~формулу~#fqw-eq-ref(<eq:thorp_absorption>)) и
Франсуа-Гаррисона (см.~формулу~#fqw-eq-ref(<eq:francois_garrison_absorption>)):

$ alpha(f) approx (0.11 f^2) / (1 + f^2) + (40 f^2) / (4100 + f^2) + (0.03 f^2) / (36 + f^2) $ <eq:thorp_absorption>
#fqw-where((
  ($alpha$, [коэффициент поглощения, дБ/км]),
  ($f$, [частота звука, кГц]),
))

#fqw-equation-list((
  (
    [$
      alpha(f) approx
      (A_1 P_1 f_1 f^2) / (f_1^2 + f^2) +
      (A_2 P_2 f_2 f^2) / (f_2^2 + f^2) +
      A_3 P_3 f^2
    $],
    <eq:francois_garrison_absorption>,
  ),
  [$ A_1 = 8.86 / c dot 10^(0.78 p H - 5), quad P_1 = 1 $],
  [$ f_1 = 2.8 sqrt(S / 35) dot 10^(4 - 1245 / (273 + T)) $],
  [$ A_2 = 21.44 S / c (1 + 0.025 T) $],
  [$ P_2 = 1 - 1.37 dot 10^(-4) D + 6.2 dot 10^(-9) D^2 $],
  [$ f_2 = (8.17 dot 10^(8 - 1990 / (273 + T))) / (1 + 0.0018 (S - 35)) $],
  [$
    A_3 = cases(
      4.937 dot 10^(-4) - 2.59 dot 10^(-5) T + 9.11 dot 10^(-7) T^2 - 1.50 dot 10^(-8) T^3 & "если " T >= 20,
      3.964 dot 10^(-4) - 1.146 dot 10^(-5) T + 1.45 dot 10^(-7) T^2 - 6.5 dot 10^(-10) T^3 & "если " T < 20
    )
  $],
  [$ P_3 = 1 - 3.83 dot 10^(-5) D + 4.9 dot 10^(-10) D^2 $],
  [$ c = 1412 + 3.21 T + 1.19 S + 0.0167 D $],
))
#fqw-where((
  ($alpha$, [коэффициент поглощения, дБ/км]),
  ($f$, [частота звука, кГц]),
  ($A_1$, [амплитудный коэффициент вклада борной кислоты]),
  ($A_2$, [амплитудный коэффициент вклада сульфата магния]),
  ($A_3$, [амплитудный коэффициент вклада чистой воды]),
  ([$P_1$, $P_2$, $P_3$], [поправочные множители на давление/глубину]),
  ($f_1$, [релаксационная частота борной кислоты, кГц]),
  ($f_2$, [релаксационная частота сульфата магния, кГц]),
  ($T$, [температура воды, °C]),
  ($S$, [солёность, ‰]),
  ($D$, [глубина, м]),
  ($p H$, [водородный показатель воды]),
  ($c$, [скорость звука в воде, м/с]),
))

Выбор формулы осуществляется пользователем.

== Требования к надёжности
<sec:reliability_requirements>
#fqw-indent-before-text

=== Требования к обеспечению надёжного функционирования программы
#fqw-indent-before-text
<sec:reliable_operation_requirements>

Некорректные критические параметры конфигурации модуля должны
выявляться с выдачей диагностических сообщений
такими средствами ns-3 как механизмы assert, abort и fatal diagnostics.

=== Время восстановления после отказа
#fqw-indent-before-text
<sec:recovery_time>

Требования к времени восстановления после отказа не предъявляются,
так как модуль является симуляционной библиотекой и
не выполняет автоматическое восстановление состояния
после критических ошибок конфигурации.

=== Отказы из-за некорректных действий пользователя
#fqw-indent-before-text
<sec:user_error_failures>

Для предотвращения отказов из-за некорректных действий пользователя в
документации должны быть описаны допустимые значения ключевых параметров.

В случае если пользователь запускает программу с некорректными значениями
программа должна завершить выполнение,
выдать диагностическое сообщение и
корректно завершить работу.

== Требования к условиям эксплуатации
<sec:operation_conditions_requirements>
#fqw-indent-before-text

=== Климатические условия эксплуатации
#fqw-indent-before-text
<sec:climate_conditions>
Требования к климатическим условиям эксплуатации не предъявляются.

=== Требования к квалификации и численности персонала
#fqw-indent-before-text
<sec:personnel_requirements>

Для эксплуатации программного модуля достаточно 1 пользователя,
обладающего квалификацией инженера-программиста и владеющего следующими
навыками:

- Навык сборки С++ проектов;

- Базовые навыки работы с библиотекой Ns-3.

== Требования к составу и параметрам технических средств
#fqw-indent-before-text
<sec:technical_means_requirements>

Для работы с программным модулем и библиотекой Ns-3 требуется
ПК со следующими характеристиками:

- Операционная система -- Linux;

- Процессор - x86-64, не менее 4 логических ядер;

- ОЗУ - не менее 8 ГБ;

- Свободное место на диске - не менее 5 ГБ \(исходный код программы,
  сборка, результаты).

== Требования к информационной и программной совместимости
<sec:compatibility_requirements>
#fqw-indent-before-text

=== Требования к информационным структурам и методам решения
#fqw-indent-before-text
<sec:information_structure_requirements>

Модуль должен поддерживать:

- Механизмы задания параметров конфигурации, принятые в библиотеке Ns-3;

- Исходный код модуля должен быть оформлен в стиле, принятом в
  Ns-3-модулях \(структура каталогов, нейминг, документирование).

=== Требования к исходным кодам и языкам программирования
#fqw-indent-before-text
<sec:source_code_requirements>

Исходный код должен представлять собой форк репозитория Ns-3 с
разработанным программным модулем.

Компилятор C++ \(gcc/clang) с поддержкой стандарта C++20.

=== Требования к программным средствам, используемым программой
#fqw-indent-before-text
<sec:software_tools_requirements>

Для работы модуля используется ns-3 с модулями
core, network, mobility, spectrum, propagation.
Для трассировки используется pcap/trace-helper.
Для сборки используется CMake/ns3 build system,
компилятор gcc или clang с поддержкой C++20.

=== Требования к защите информации
#fqw-indent-before-text
<sec:information_security_requirements>

Требования к защите информации не предъявляются
(модуль не реализует механизмы аутентификации/авторизации и
не обрабатывает персональные данные).

== Требования к маркировке и упаковке
#fqw-indent-before-text
<sec:labeling_packaging_requirements>

Требования к маркировке и упаковке не предъявляются.
Поставка программного модуля осуществляется в виде форка библиотеки Ns-3
с разработанным программным модулем.

== Требования к транспортированию и хранению
#fqw-indent-before-text
<sec:transportation_storage_requirements>

Хранение исходного кода программы должно быть выполнено с помощью
системы контроля версий Git.

== Специальные требования
#fqw-indent-before-text
<sec:special_requirements>
При разработке программного модуля необходимо
использовать стандартные интерфейсы, используемые в Ns-3
для повышения совместимости с такими модулями, как
network, propagation, mobility, spectrum.

= Требования к программной документации
#fqw-indent-before-text
<sec:documentation_requirements>

Состав разрабатываемой программной документации должен включать в себя:

- Техническое задание, оформленное в соответствии с Учебно-методическим
  пособием «Подготовка, оформление выпускной квалификационной работы и
  преддипломной практики»;

- Пояснительную записку, оформленную в соответствии с
  Учебно-методическим пособием «Подготовка, оформление выпускной
  квалификационной работы и преддипломной практики»;

- Руководство системного программиста, оформленное в соответствии с
  учебно-методическим пособием «Подготовка, оформление выпускной
  квалификационной работы и преддипломной практики».

= Технико-экономические показатели
<sec:technical_economic_indicators>
#fqw-indent-before-text

== Экономические преимущества разработки
#fqw-indent-before-text
<sec:economic_advantages>

Разработка программного модуля модели
программно-аппаратного гидроакустического приёмопередатчика для
проведения виртуальных испытаний и
подготовительных исследований для последующих полунатурных испытаний
обеспечивает сокращение временных и финансовых затрат на
создание и отработку систем гидроакустической связи.

Применение компьютерного моделирования позволяет
перенести значительную часть проверок и исследований
с этапа изготовления опытного образца на этап проектирования.
Это снижает объём натурных испытаний,
требующих привлечения специализированного оборудования,
испытательных акваторий и
обслуживающего персонала,
что приводит к уменьшению прямых эксплуатационных расходов.

Использование модели на ранних стадиях разработки обеспечивает
выявление архитектурных и алгоритмических ошибок
до изготовления аппаратной части.
В результате сокращаются затраты,
связанные с внесением изменений в опытные образцы,
переработкой конструкторской документации и
повторным проведением испытаний.

Реализация возможности многократного воспроизведения
сценариев эксплуатации в виртуальной среде позволяет
ускорить процесс отработки алгоритмов функционирования
мультиагентных систем и
оптимизации параметров связи.
Это способствует сокращению общего срока разработки и
повышению качества проектных решений.

Таким образом, внедрение разработанного программного модуля обеспечивает
снижение совокупной стоимости разработки гидроакустических систем связи,
уменьшение сроков создания опытных образцов и
повышение эффективности проведения испытаний.

= Стадии и этапы разработки
<sec:development_stages>
#fqw-indent-before-text

== Стадии разработки
#fqw-indent-before-text
<sec:development_stages_list>

Разработка проходит в шесть этапов в соответствии с таблицей
#fqw-table-ref(<tab:development_stages>).

#fqw-table(
  [Стадии разработки],
  columns: (2fr, 1fr, 2fr),
  label: <tab:development_stages>,
  align: left + horizon,
  header: (
    [*Название этапа*],
    [*Сроки*],
    [*Артефакт\(-ы)*],
  ),
  rows: (
    [Анализ требований и предметной области],
    [05.09.2025 -- 30.09.2025],
    [Неформализованный набор требований],
    [Формализация требований],
    [01.10.2025 -- 31.10.2025],
    [Техническое задание],
    [Проектирование модуля],
    [01.11.2025 -- 30.11.2025],
    [Диаграммы классов, использования и т.д.],
    [Разработка программного модуля],
    [01.12.2025 -- 28.02.2026],
    [Рабочий прототип гидроакустического модуля],
    [Тестирование модуля],
    [01.03.2026 -- 30.04.2026],
    [Журнал тестирования],
    [Документирование],
    [01.05.2026 -- 31.05.2026],
    [Пояснительная записка],
  ),
)

== Содержание работ по этапам
#fqw-indent-before-text
<sec:stage_content>

На стадии анализа требований и предметной области должны быть выполнены
следующие виды работ:
- Сбор требований;
- Исследование предметной области;
- Разработка технического задания.

На стадии проектирования модуля должны быть выполнены:
- Разработка диаграммы вариантов использования;
- Разработка диаграммы классов проектного уровня.

На стадии разработки модуля должно быть выполнено кодирование модуля.

На стадии тестирования модуля должен быть протестирован функционал модуля.

На стадии документирования должна быть выполнена
разработка пояснительной записки.

= Порядок контроля и приёмки
<sec:control_acceptance>
#fqw-indent-before-text

== Виды испытаний
#fqw-indent-before-text
<sec:testing_types>

Программа сдаётся на проверку заказчику 05.06.2026.
При обнаружении в программе ошибок или недостатков
исполнитель обязуется устранить их в недельный срок и
предоставить программу на повторную проверку.

Программа сдаётся на проверку независимому тестировщику
не позднее 09.06.2026.
Результаты тестирования предоставляются на защите дипломного проекта
членам ГАК.

#fqw-appendix-group(number: 2)

#fqw-subappendix(
  [Диаграмма вариантов использования],
  label: <sec:appendix_b_1>,
  title-label: <sec:appendix_b_1_use_case_diagram>,
  next-paragraph: true,
)

На рисунке~#fqw-figure-ref(<fig:use-case-diagram>)
представлена диаграмма вариантов использования.

#fqw-figure(
  image("../assets/images/technical-assignment/use-case-diagram.png"),
  [Диаграмма вариантов использования «Use Case».],
) <fig:use-case-diagram>

#fqw-subappendix(
  [Сценарии вариантов использования],
  label: <sec:appendix_b_2>,
  title-label: <sec:appendix_b_2_use_case_scenarios>,
  next-paragraph: true,
)

#block()[
  #set enum(
    full: true,
  )
  + Запуск симуляции с двумя узлами
    + Параметры по умолчанию

      - Пользователь запускает сборку тестового примера «передача сообщения
        между двумя узлами с гидроакустическими приёмопередатчиками».

      - Пользователь запускает программу без параметров командной строки.

      - Если межузловое расстояние не задано, используется значение по
        умолчанию 1~км.

      - Программа формирует отчёт симуляции, содержащий:

        - параметры сценария, включая межузловое расстояние по умолчанию;

        - время отправки сообщения;

        - результат приёма сообщения (успех);

        - время приёма сообщения;

        - адрес устройства-отправителя;

        - адрес устройства-получателя;

        - полезную нагрузку сообщения;

        - среднее значение ОСШ.

      - Сообщение принято успешно.

    + Увеличенное межузловое расстояние

      - Пользователь запускает сборку тестового примера «передача сообщения
        между двумя узлами с гидроакустическими приёмопередатчиками».

      - Пользователь запускает программу с параметром командной строки,
        задающим межузловое расстояние в 10 км.

      - Программа формирует отчёт симуляции, содержащий:

        - параметры сценария, включая пользовательское значение межузлового
          расстояния;

        - результат приёма сообщения (неудача).

      - При достаточно большом межузловом расстоянии сообщение не принимается
        успешно.

  + Запуск симуляции с тремя узлами
    + Одновременная передача двумя узлами-отправителями при одинаковом
      расстоянии до узла-получателя

      - Пользователь запускает сборку тестового примера «одновременная
        передача сообщений двумя узлами-отправителями одному узлу-получателю».

      - Пользователь запускает программу без параметров командной строки.

      - Если межузловые расстояния не заданы, используются значения по
        умолчанию, при которых оба узла-отправителя находятся на одинаковом
        расстоянии от узла-получателя.

      - Программа формирует отчёт симуляции, содержащий:

        - параметры сценария, включая межузловые расстояния по умолчанию;

        - время отправки первого сообщения;

        - время отправки второго сообщения;

        - результат приёма первого сообщения (неудача);

        - результат приёма второго сообщения (неудача);

        - среднее значение ОСШ для сообщения, если оно доступно;

        - среднее значение ОСШ для сообщения, если оно доступно.

      - При одновременной передаче сообщений от двух узлов-отправителей,
        находящихся на одинаковом расстоянии от узла-получателя, оба сообщения
        не принимаются успешно из-за взаимной интерференции.

    + Одновременная передача двумя узлами-отправителями при разном
      расстоянии до узла-получателя

      - Пользователь запускает сборку тестового примера «одновременная
        передача сообщений двумя узлами-отправителями одному узлу-получателю».

      - Пользователь запускает программу с параметрами командной строки,
        задающими межузловые расстояния от каждого узла-отправителя до
        узла-получателя.

      - Программа формирует отчёт симуляции, содержащий:

        - параметры сценария, включая пользовательские значения межузловых
          расстояний;

        - время отправки первого сообщения;

        - время отправки второго сообщения;

        - результат приёма первого сообщения (успех);

        - время приёма первого сообщения;

        - среднее значение ОСШ для первого сообщения, если оно доступно;

        - результат приёма второго сообщения (неудача);

        - среднее значение ОСШ для второго сообщения, если оно доступно.

      - При одновременной передаче сообщений от двух узлов-отправителей,
        находящихся на разном расстоянии от узла-получателя, сообщение от
        ближнего узла принимается успешно, а сообщение от дальнего узла не
        принимается успешно.
]
#fqw-subappendix(
  [Макеты экранных форм],
  label: <sec:appendix_b_3>,
  title-label: <sec:appendix_b_3_screen_mockups>,
  next-paragraph: true,
)

Макеты экранных форм работы модуля представлены
на рисунках #fqw-figure-ref(<fig:wireshark-trace>)~--~#fqw-figure-ref(<fig:three-nodes-output>).

#fqw-figure(
  image("../assets/images/technical-assignment/wireshark-trace.png", width: 140mm),
  [Отображение результатов трассировки в программе Wireshark],
) <fig:wireshark-trace>

#fqw-figure(
  image("../assets/images/technical-assignment/two-nodes-output.png", width: 140mm),
  [Отображение результатов работы сценария с двумя узлами],
) <fig:two-nodes-output>

#fqw-figure(
  image("../assets/images/technical-assignment/three-nodes-output.png", width: 140mm),
  [Отображение результатов работы сценария с тремя узлами],
) <fig:three-nodes-output>

#fqw-subappendix(
  [Структура и формат данных],
  label: <sec:appendix_b_4>,
  title-label: <sec:appendix_b_4_data_structure>,
  next-paragraph: true,
)

#fqw-subappendix-section(
  [Взаимодействие двух узлов],
  label: <sec:appendix_b_4_1>,
)

#fqw-subappendix-subsection([Входные данные])

Входные данные представляют собой параметры командной строки и
представлены в таблице #fqw-table-ref(<tabl:appendix_b_4_1>):

#fqw-table(
  [Входные данные сценария взаимодействия двух узлов],
  columns: (1.3fr, 1.5fr, 1fr, 1fr, 1fr),
  align: left + horizon,
  inset: 4pt,
  label: <tabl:appendix_b_4_1>,
  header: (
    [Параметр командной строки],
    [Назначение],
    [Тип данных],
    [Единица измерения],
    [Значение по умолчанию],
  ),
  rows: (
    [--sound-speed], [Скорость распространения звука в среде], [вещественное число], [м/с], [1500],
    [--geometric-coefficient], [Коэффициент геометрического расхождения], [вещественное число], [нет], [2.0],
    [--absorption-model], [Имя модели поглощения сигнала], [строка], [нет], [ns3::Francois#linebreak()Garrison#linebreak()Propagation#linebreak()LossModel],
    [--snr-threshold],
    [Минимальное среднее значение ОСШ, необходимое для успешного приёма],
    [вещественное число],
    [нет],
    [10],
    [--distance],
    [Межузловое расстояние между узлом-отправителем и узлом-получателем],
    [вещественное число],
    [м],
    [1000],
  ),
)

#fqw-subappendix-subsection([Выходные данные])

Выходные данные формируются в текстовом виде и выводятся в стандартный
поток вывода \(stdout) и имеют следующий вид:

_
Simulation with 2 nodes.                            #parbreak()
Parameters:                                         #parbreak()
Inter-node distance: \<значение\> m                 #parbreak()
Sound speed: \<значение\> m/s                       #parbreak()
Geometric spreading loss coefficient: \<значение\>  #parbreak()
Absorption loss model: \<значение\>                 #parbreak()
SNR threshold: \<значение\>                         #parbreak()
Carrier frequency: \<значение\> Hz                  #parbreak()
Bandwidth: \<значение\> Hz                          #parbreak()
Power: \<значение\> uPa^2                           #parbreak()
PSD: \<значение\> uPa^2/Hz                          #parbreak()
Send time: \<значение\> ms                          #parbreak()
Receive result: success|failure
_

При успешном приёме дополнительно выводятся:

_
Receive time: \<значение\> ms   #parbreak()
Sender address: \<значение\>    #parbreak()
Receiver address: \<значение\>  #parbreak()
Payload: \<значение\>           #parbreak()
Mean SNR: \<значение\>
_
#fqw-indent-before-text

При неуспешном приёме дополнительно выводится:

_Message was not received successfully._

#fqw-subappendix-section(
  [Взаимодействие трёх узлов],
  label: <sec:appendix_b_4_2>,
)

#fqw-subappendix-subsection([Входные данные])

Входные данные представляют собой параметры командной строки и
представлены в таблице #fqw-table-ref(<tabl:appendix_b_4_2>)

#fqw-table(
  [Входные данные сценария взаимодействия трёх узлов],
  columns: (1.3fr, 2fr, 1.2fr, 0.9fr, 1.7fr),
  align: left + horizon,
  inset: 4pt,
  label: <tabl:appendix_b_4_2>,
  header: (
    [Параметр командной строки],
    [Назначение],
    [Тип данных],
    [Единица измерения],
    [Значение по умолчанию],
  ),
  rows: (
    [--sound-speed], [Скорость распространения звука в среде], [вещественное число], [м/с], [1500],
    [--geometric-coefficient], [Коэффициент геометрического расхождения], [вещественное число], [нет], [2.0],
    [--absorption-model], [Имя модели поглощения сигнала], [строка], [нет], [ns3::Francois#linebreak()Garrison#linebreak()Propagation#linebreak()LossModel],
    [--snr-threshold],
    [Минимальное среднее значение ОСШ, необходимое для успешного приёма],
    [вещественное число],
    [нет],
    [10],
    [--sender1-distance],
    [Расстояние от первого узла-отправителя до узла-получателя],
    [вещественное число],
    [м],
    [1000],
    [--sender2-distance],
    [Расстояние от второго узла-отправителя до узла-получателя],
    [вещественное число],
    [м],
    [1000],
  ),
)

#fqw-subappendix-subsection([Выходные данные])

Выходные данные формируются в текстовом виде и выводятся в стандартный
поток вывода \(stdout) и имеют следующий вид:

_
Simulation with 3 nodes.                            #parbreak()
Parameters:                                         #parbreak()
Sender 1 distance: \<значение\> m                   #parbreak()
Sender 2 distance: \<значение\> m                   #parbreak()
Sound speed: \<значение\> m/s                       #parbreak()
Geometric spreading loss coefficient: \<значение\>  #parbreak()
Absorption loss model: \<значение\>                 #parbreak()
SNR threshold: \<значение\>                         #parbreak()
Carrier frequency: \<значение\> Hz                  #parbreak()
Bandwidth: \<значение\> Hz                          #parbreak()
Power: \<значение\> uPa^2                           #parbreak()
PSD: \<значение\> uPa^2/Hz                          #parbreak()
Message 1:                                          #parbreak()
Send time: \<значение\> ms                          #parbreak()
Receive result: success|failure                     #parbreak()
Mean SNR: \<значение\>|not available                #parbreak()
Message 2:                                          #parbreak()
Send time: \<значение\> ms                          #parbreak()
Receive result: success|failure                     #parbreak()
Mean SNR: \<значение\>|not available                #parbreak()
_
#fqw-indent-before-text

При успешном приёме для каждого сообщения дополнительно выводятся:

_
Receive time: \<значение\> ms   #parbreak()
Sender address: \<значение\>    #parbreak()
Receiver address: \<значение\>  #parbreak()
Payload: \<значение\>           #parbreak()
_
#fqw-indent-before-text

При неуспешном приёме дополнительно выводится:

_Message was not received successfully.#parbreak()_
#fqw-indent-before-text

#fqw-subappendix(
  [Состояния физического уровня модели приёмопередатчика],
  label: <sec:appendix_b_5>,
  title-label: <sec:appendix_b_5_phy_state_machine>,
  next-paragraph: true,
)

На рисунке~#fqw-figure-ref(<fig:phy-state-machine>) представлена диаграмма машины состояний.

#fqw-figure(
  image("../assets/images/technical-assignment/phy-state-machine.png", width: 140mm),
  [Диаграмма состояний физического уровня модели приёмопередатчика],
) <fig:phy-state-machine>
